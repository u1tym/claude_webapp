# password-management フロントの配置例。
# 公開 URL は /portal_password_management/ 。Vite base も同じ。
# dist の中身はドキュメントルートの features/password-management/ に置く。
#
#   ドキュメントルート/features/password-management/index.html
#   ドキュメントルート/features/password-management/assets/...
#
# rewrite は last（break ではない）。

server {
    listen 80;
    server_name example.com;

    root /var/www/html;

    location = /portal_password_management {
        return 301 /portal_password_management/;
    }

    location /portal_password_management/ {
        rewrite ^/portal_password_management/(.*)$ /features/password-management/$1 last;
    }

    location /features/password-management/ {
        internal;
        try_files $uri /features/password-management/index.html;
    }

    # API を同一オリジンにする場合。使わないならこの location は不要。
    # フロントの VITE_API_PASSWORD_MANAGEMENT_URL は https://example.com/portal-password-management-api にする。
    location /portal-password-management-api/ {
        proxy_pass http://127.0.0.1:8004/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
