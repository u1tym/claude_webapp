# goods-management フロントの配置例。
# 公開 URL は /portal_goods_management/ 。Vite base も同じ。
# dist の中身はドキュメントルートの features/goods-management/ に置く。
#
#   ドキュメントルート/features/goods-management/index.html
#   ドキュメントルート/features/goods-management/assets/...
#
# rewrite は last（break ではない）。

server {
    listen 80;
    server_name example.com;

    root /var/www/html;

    location = /portal_goods_management {
        return 301 /portal_goods_management/;
    }

    location /portal_goods_management/ {
        rewrite ^/portal_goods_management/(.*)$ /features/goods-management/$1 last;
    }

    location /features/goods-management/ {
        internal;
        try_files $uri /features/goods-management/index.html;
    }

    # API を同一オリジンにする場合。使わないならこの location は不要。
    # フロントの VITE_API_GOODS_MANAGEMENT_URL は https://example.com/portal-goods-management-api にする。
    location /portal-goods-management-api/ {
        proxy_pass http://127.0.0.1:8006/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
